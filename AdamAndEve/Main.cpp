#include "GameMap.h"
#include <chrono>
#include <thread>

int main() {
	GameMap map = GameMap();

	//while (1) {
	//	map.DisplayMap();
	//	std::this_thread::sleep_for(std::chrono::milliseconds(200));
	//}

}